name: Workflow filters and activities

on:
    workflow_dispatch: 
    # schedule:
    #     - cron: "*/59 * * * *"
    push:
        branches: 
            - main
            - '!feature/*' #ignore pushing to any branch starting with feature using !
        # branches-ignore: 
        #     - feature/*
        #     - test/**
    pull_request: 
        types: 
            - opened
            - closed
        paths-ignore: 
            - README.md
        branches: 
            - main
        

jobs:
    hello:
        runs-on: ubuntu-latest 
        steps:
        - run: echo this workflow/jobs/step is excuted for event type - ${{ github.event_name }}